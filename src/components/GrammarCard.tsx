export default function GrammarCard() { return <article className="native-card"><p className="text-sm font-semibold uppercase tracking-[.25em] text-blue-600">SIT grammar</p><h2 className="mt-2 text-2xl font-semibold">Sentence → Expression → Token → Native SIT</h2><div className="mt-5 grid gap-3 text-center sm:grid-cols-5">{['Subject', 'Verb', 'Object', 'Modifier', 'Connector'].map((item) => <div key={item} className="rounded-2xl bg-blue-50 px-3 py-4 font-medium text-blue-800 dark:bg-blue-950/40 dark:text-blue-200">{item}</div>)}</div><p className="mt-5 text-slate-600 dark:text-slate-300">Tokens express concepts, actions, relations and modifiers. The grammar deliberately leaves room for future complete semantic expressions.</p></article> }
